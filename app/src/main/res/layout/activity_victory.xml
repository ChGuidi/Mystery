<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_victory"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/bgpressed"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="cs.nuim.maps.VictoryActivity">

    <TextView
        android:layout_centerHorizontal="true"
        android:id="@+id/congratulations"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Congratulations!"
        android:textSize="30dp"/>
    <TextView
        android:id="@+id/breaktxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/congratulations"
        android:paddingBottom="10dp"
        android:text="BREAKING: BRILLIANT STUDENT SOLVES MURDER"/>

    <ImageView
        android:id="@+id/newsimage"
        android:background="@drawable/newspaper"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/exitbuttonend"
        android:layout_below="@id/breaktxt"
        android:adjustViewBounds="true"/>

    <Button
        android:id="@+id/newgamebutton"
        android:onClick="startNewGame"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New Game"/>

    <Button
        android:id="@id/exitbuttonend"
        android:onClick="goToMap"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/newgamebutton"
        android:layout_height="wrap_content"
        android:text="Exit"/>
</RelativeLayout>
